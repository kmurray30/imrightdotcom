You are given a topic and a list of argument angles, each with search_queries. Your task is to evaluate each angle for likelihood to land on Wikipedia.

For EACH angle, you must provide:
1. filtering_thought: Your chain-of-thought reasoning. Explain why this angle is likely or unlikely to land. Consider: Are the queries broad or hyper-specific? Do we know the building-block facts exist? Does it presuppose the conclusion?
2. keep: true if the angle is likely to land, false otherwise.

FAVOR (keep: true):
- Broad fishing: queries that cast a wide net (e.g. "coldest days on record", "California electrical outages")
- Known-to-be-true building blocks: queries for facts we know exist (e.g. "Russia military dolphins", "CIA acoustic kitty")
- Queries that find each piece of evidence separately rather than assuming a direct correlation exists

DISFAVOR (keep: false):
- Specific-fishing for stuff we don't know is true (e.g. "squirrels carrying objects videos", "EVs causing power outages")
- Queries that presuppose the conclusion or a specific unverifiable claim
- Arguments that are weak or uncompelling even if the queries might land

Return a JSON array. Each object must have exactly: argument, search_queries, filtering_thought, keep.

Example:
[
  {
    "argument": "squirrel populations exploded in the US after the Cold War, suspiciously timed with Russian intelligence ops",
    "search_queries": ["squirrel population growth US history", "Russian intelligence operations timeline"],
    "filtering_thought": "Relies on finding squirrel population data over time, as well as expecting there to be a specific spike around the cold war. Both are presumptuous and unlikely to be fruitful.",
    "keep": false
  }
]

Output nothing else—no explanation, no markdown wrapper. Just the JSON array.
