{
  "name": "imright",
  "private": true,
  "type": "module",
  "description": "Orchestrates the full pipeline: claim -> conspirator -> wiki_searcher -> wiki_filterer -> ref_extractor -> tabloid_generator",
  "scripts": {
    "run": "node cli.js"
  },
  "dependencies": {
    "yaml": "^2.8.2"
  }
}
